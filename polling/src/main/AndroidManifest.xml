<manifest package="cn.mycommons.polling"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">

        <service
            android:name=".PollingServer"
            android:exported="false" />
    </application>

</manifest>